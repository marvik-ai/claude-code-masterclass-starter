{
  "name": "workshop-api",
  "version": "1.0.0",
  "description": "Claude Code Masterclass starter project — Express REST API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "jest --testEnvironment node --forceExit"
  },
  "dependencies": {
    "express": "^4.18.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
